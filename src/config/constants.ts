/**
 * Configuration constants for the ESM module system
 */

/**
 * Maximum allowed size for module code in bytes (1MB)
 */
export const MAX_CODE_SIZE = 1024 * 1024

/**
 * Default number of entries in the module cache
 */
export const DEFAULT_CACHE_SIZE = 1000

/**
 * Cache time-to-live in milliseconds (5 minutes)
 */
export const CACHE_TTL_MS = 5 * 60 * 1000
